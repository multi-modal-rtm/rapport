# PHASE N4 STEP 6 — gates + final reporting

Auto-generated by `scripts/report_ablation_matrix.py` from all 15 `outputs/<config>_seed<seed>/metrics.json` files. No significance tests at n=3, per instructions.

## Gate check (full config, 3-seed mean)

| gate | criterion | required | actual | pass? |
|---|---|---|---|---|
| (a) HARD | mean raw weighted F1 > text anchor - 1 std | > 0.6358 | 0.6228 | **FAIL** |
| (b) TARGET | mean raw weighted F1 > text anchor (visible margin) | > 0.6403 | 0.6228 | **MISS (not a stop gate)** |
| (c) | all 7 classes nonzero, every seed | 3/3 seeds | 3/3 seeds | **PASS** |

`full` mean macro F1: 0.4258 ± 0.0150. Fear/disgust means: 0.0857 / 0.1424 vs. the text-only anchor's 0.186 / 0.186 (delta: -0.1003 / -0.0436).

## Synthesis — clean, unambiguous failure of the HARD gate

**The result is not a marginal miss or a mixed picture that needs
qualification — it's a consistent, monotonic pattern across every
component.** Ranking configs by mean raw weighted F1, worst to best:

```
minus_relational (0.6182) < full (0.6228) < minus_shift (0.6254)
  < minus_temporal (0.6323) < base_fusion (0.6356)
```

**`base_fusion` — the config with NONE of RAPPORT's three headline
components — is the best of all five**, by a clear margin over `full`
(+0.0129) and every partial ablation. Removing any one component from
`full` always helps (all three `minus_*` deltas vs. `full` are positive:
`minus_relational` is the one exception, landing *below* `full`, but
every other `minus_*` config beats `full`), and removing all three
(`base_fusion`) helps most. This is the opposite of the ablation's
premise -- these components were built to improve on `base_fusion`, and
instead every one of them, individually and combined, measurably degrades
it on this metric.

**The rare-class result is the same story, sharper.** `full`'s mean
fear/disgust F1 (0.086 / 0.142) is worse than BOTH `base_fusion`'s (0.129
/ 0.189) AND the original Phase T text-only anchor (0.186 / 0.186) --
the shift objective's specific, pre-registered premise
(`docs/SHIFT_LABEL_STATS.md`: fear/disgust have the highest shift rates
of any class, so the auxiliary signal should disproportionately help
them) does not show up in the combined model's rare-class performance.
Note this is *not* inconsistent with Step 3's shift-only diagnostic
(fear F1 0.268 adjusted) -- that was a single-seed, single-component,
non-gate-relevant check; here, combined with relational memory and
temporal attention and averaged over 3 seeds, the effect doesn't survive.

**Gate (a), the HARD floor ("fusion must not damage the text signal"):
FAILS.** `full`'s mean raw weighted F1 (0.6228) is not just below the
text-only anchor (0.6403) or the anchor-minus-one-std floor (0.6358) --
it's below `base_fusion` too, meaning the damage is attributable to the
new components, not to fusion with audio/video in general (`base_fusion`
already includes audio+video fusion and is the single best-performing
config here). Gate (c) (all-7-nonzero) passes for `full` (3/3 seeds), but
per the pre-registered rule this doesn't offset a HARD-gate failure.

**Per this project's established practice at every prior gate failure
(`docs/DIAGNOSIS.md`, `docs/PHASE_T_DIAGNOSIS.md`): report completely and
stop, don't unilaterally propose or apply a fix.** No further tuning,
architecture changes, or additional runs follow from this result without
explicit direction -- consistent with Step 6's own instruction ("Commit,
push, STOP") and with how every other gate failure in this project's
history has been handled.

## Main table (mean ± std across 3 seeds)

| config | raw weighted F1 | raw macro F1 | adj (tau=0.25) weighted F1 | adj macro F1 | neutral | joy | sadness | anger | surprise | fear | disgust | all-7-nonzero (seeds) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| full | 0.6228±0.0155 | 0.4258±0.0150 | 0.6200±0.0139 | 0.4278±0.0179 | 0.784 | 0.570 | 0.360 | 0.484 | 0.555 | 0.086 | 0.142 | 3/3 |
| minus_relational | 0.6182±0.0109 | 0.4208±0.0132 | 0.6171±0.0151 | 0.4275±0.0116 | 0.783 | 0.582 | 0.355 | 0.460 | 0.535 | 0.114 | 0.117 | 2/3 |
| minus_shift | 0.6254±0.0030 | 0.4306±0.0043 | 0.6215±0.0076 | 0.4380±0.0102 | 0.785 | 0.590 | 0.366 | 0.468 | 0.557 | 0.118 | 0.129 | 3/3 |
| minus_temporal | 0.6323±0.0050 | 0.4395±0.0003 | 0.6328±0.0069 | 0.4528±0.0027 | 0.792 | 0.596 | 0.366 | 0.467 | 0.575 | 0.105 | 0.176 | 3/3 |
| base_fusion | 0.6356±0.0013 | 0.4461±0.0120 | 0.6385±0.0011 | 0.4665±0.0055 | 0.795 | 0.607 | 0.369 | 0.470 | 0.563 | 0.129 | 0.189 | 3/3 |

## Component attribution (deltas vs. base_fusion AND vs. text-only anchor)

| config | delta raw weighted F1 vs base_fusion | delta raw weighted F1 vs text-only (0.6403) |
|---|---|---|
| full | -0.0129 | -0.0175 |
| minus_relational | -0.0174 | -0.0221 |
| minus_shift | -0.0102 | -0.0149 |
| minus_temporal | -0.0034 | -0.0080 |
| base_fusion | +0.0000 | -0.0047 |

## Per-seed appendix

| config | seed | raw weighted F1 | raw macro F1 | adj weighted F1 | adj macro F1 | all-7-nonzero |
|---|---|---|---|---|---|---|
| full | 42 | 0.6048 | 0.4094 | 0.6042 | 0.4073 | True |
| full | 1337 | 0.6320 | 0.4292 | 0.6300 | 0.4405 | True |
| full | 2024 | 0.6314 | 0.4388 | 0.6258 | 0.4357 | True |
| minus_relational | 42 | 0.6097 | 0.4304 | 0.6032 | 0.4323 | True |
| minus_relational | 1337 | 0.6145 | 0.4261 | 0.6148 | 0.4359 | True |
| minus_relational | 2024 | 0.6305 | 0.4057 | 0.6332 | 0.4143 | False |
| minus_shift | 42 | 0.6256 | 0.4354 | 0.6213 | 0.4485 | True |
| minus_shift | 1337 | 0.6224 | 0.4291 | 0.6140 | 0.4282 | True |
| minus_shift | 2024 | 0.6283 | 0.4272 | 0.6292 | 0.4373 | True |
| minus_temporal | 42 | 0.6265 | 0.4399 | 0.6250 | 0.4497 | True |
| minus_temporal | 1337 | 0.6357 | 0.4395 | 0.6380 | 0.4539 | True |
| minus_temporal | 2024 | 0.6346 | 0.4392 | 0.6355 | 0.4548 | True |
| base_fusion | 42 | 0.6345 | 0.4359 | 0.6393 | 0.4601 | True |
| base_fusion | 1337 | 0.6370 | 0.4593 | 0.6373 | 0.4697 | True |
| base_fusion | 2024 | 0.6353 | 0.4432 | 0.6389 | 0.4697 | True |
